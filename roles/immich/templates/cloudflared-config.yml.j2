# {{ ansible_managed }}
tunnel: {{ immich_tunnel_id }}
credentials-file: /etc/cloudflared/credentials.json

ingress:
  - hostname: {{ immich_domain }}
    service: http://127.0.0.1:{{ immich_port }}
  - service: http_status:404
