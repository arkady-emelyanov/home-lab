---
- name: Restart gatus
  # Stop is `docker compose down`, so a restart recreates the container --
  # which is what a changed config or compose file needs.
  ansible.builtin.systemd_service:
    name: gatus
    state: restarted
    daemon_reload: true
